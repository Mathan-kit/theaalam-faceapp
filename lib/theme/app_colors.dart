import 'package:flutter/material.dart';

class AppColors {
  // Primary & Backgrounds (Light Milky Yellow & Golden Amber Palette matching logo.png)
  static const Color primary = Color(0xFFEA580C); // Deep Sunset Orange / Amber
  static const Color secondary = Color(0xFF292524); // Warm Dark Espresso / Charcoal
  static const Color background = Color(0xFFFFFDF5); // Light Milky Yellow / Warm Cream
  static const Color surface = Color(0xFFFFFFFF); // Pure White Surface
  static const Color surfaceElevated = Color(0xFFFFFFFF);
  static const Color surfaceSubtle = Color(0xFFFFFBEB); // Milky Soft Amber-50

  // Accents & Brand Identity (Golden Yellow & Warm Orange)
  static const Color accent = Color(0xFFF59E0B); // Golden Amber / Honey Yellow
  static const Color accentDark = Color(0xFFD97706); // Rich Amber
  static const Color accentLight = Color(0xFFFEF3C7); // Light Milky Yellow (Amber-100)
  static const Color secondaryAccent = Color(0xFFFB923C); // Warm Tangerine
  static const Color secondaryAccentLight = Color(0xFFFFEDD5); // Soft Peach Cream (Orange-50)

  // Semantic (All following Warm Orange/Golden Yellow identity)
  static const Color success = Color(0xFFD97706); // Warm Amber Gold (for Check-In)
  static const Color successLight = Color(0xFFFEF3C7); // Light Milky Gold
  static const Color warning = Color(0xFFF59E0B); // Warm Golden Yellow
  static const Color warningLight = Color(0xFFFFFBEB); // Soft Amber
  static const Color error = Color(0xFFC2410C); // Deep Terracotta / Rust Orange (for Check-Out)
  static const Color errorLight = Color(0xFFFFEDD5); // Soft Peach Cream

  // Text
  static const Color textPrimary = Color(0xFF1C1917); // Dark Warm Slate
  static const Color textSecondary = Color(0xFF78716C); // Warm Muted Stone
  static const Color textMuted = Color(0xFFA8A29E); // Light Warm Gray
  static const Color textOnDark = Color(0xFFFFFFFF);
  static const Color textAccent = Color(0xFFB45309); // Deep Golden Brown

  // Borders & Dividers
  static const Color border = Color(0xFFFDE68A); // Soft Golden Cream Border
  static const Color borderSubtle = Color(0xFFFEF9C3); // Very Light Milky Yellow Border
  static const Color borderFocus = Color(0xFFF59E0B); // Golden Focus Ring
}
